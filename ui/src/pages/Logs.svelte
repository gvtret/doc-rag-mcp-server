<script lang="ts">
  // Stub page — content lands in v2.2.4.
</script>

<section>
  <p class="muted">Раздел появится в v2.2.4.</p>
  <p>
    Здесь будет живой просмотр логов ingest/rebuild и HTTP-запросов
    (<code>log_tail</code> + <code>http_log_tail</code> из
    <code>/ui/status</code>), фильтр по уровню и поиск по строкам.
  </p>
</section>

<style>
  section {
    padding: 16px 24px;
    color: var(--text-secondary);
    max-width: 720px;
  }
  .muted {
    color: var(--text-muted);
  }
</style>
