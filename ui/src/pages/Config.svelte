<script lang="ts">
  // Stub page — content lands in v2.2.2.
</script>

<section>
  <p class="muted">Раздел появится в v2.2.2.</p>
  <p>
    Здесь будет отображение и редактирование <code>config/config.yaml</code>:
    параметры парсинга (pdf_backend, docx_backend, edition_year),
    чанкинга (target_tokens, overlap_tokens, dedup_similarity_threshold),
    эмбеддингов (model_name, device, batch_size), индекса и сервера.
  </p>
</section>

<style>
  section {
    padding: 16px 24px;
    color: var(--text-secondary);
    max-width: 720px;
  }
  .muted {
    color: var(--text-muted);
  }
</style>
